# Configure npm to use HTTPS instead of SSH for Git operations
# This provides additional protection against SSH authentication issues
git-protocol=https

# Network timeout settings
timeout=100000

# Registry settings
registry=https://registry.npmjs.org/ 